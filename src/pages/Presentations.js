import React from 'react';
import { Gallery } from '../components';
import { presentations } from '../data';

const Presentations = () => <Gallery slides={presentations} />;

export default Presentations;
